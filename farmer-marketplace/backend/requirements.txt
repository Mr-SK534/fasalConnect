# ============================================================
# Farmer Marketplace — Backend Dependencies
# Team Zenith — SIH Project
#
# Install with:
#   pip install -r requirements.txt --break-system-packages
# (drop --break-system-packages if using a virtual environment)
# ============================================================

# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database / ORM ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.12
alembic==1.13.3

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# --- Validation (Pydantic, ships with FastAPI, pinned for consistency) ---
pydantic==2.9.2
email-validator==2.2.0

# --- Environment variables ---
python-dotenv==1.0.1

# --- Demand Forecasting ---
prophet==1.1.6
pandas==2.2.3
scikit-learn==1.5.2

# --- Route Optimization ---
ortools==9.11.4210

# --- Payments ---
razorpay==1.4.2

# --- WhatsApp Notifications ---
twilio==9.3.2

# --- Testing (optional but recommended) ---
pytest==8.3.3
httpx==0.27.2